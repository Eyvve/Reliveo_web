import React, {useEffect} from 'react'

function CreateUser() 

    {useEffect(() => {
        document.title = "Reliveo | Créer utilisateur"
    }, []);

  return (
    <div>CreateUser</div>
  )
}

export default CreateUser